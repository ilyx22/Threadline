# Decisions log — public experience and completion pass

Append-only. Each entry: date, decision, why, consequence.

| Date | Decision | Why | Consequence |
|---|---|---|---|
| 2026-09-09 | **Birdhouse is a specimen, not a spec.** Reference analysed mechanically (20 widths, computed styles, geometry, motion); principles extracted; every asset, colour, character, framework, claim and sentence refused. | The founder asked for the rigour of a reconstruction system with a different objective: an original Threadline experience. | `reference-analysis/` holds the evidence; `forbidden-to-copy.md` is enforced by a grep in `qa:public`. |
| 2026-09-09 | **Public visual direction: warm light canvas, editorial serif + plain sans + mono labels, paper cards with ink outlines and hard offset shadows.** | Distinct from the reference (sky blue / navy / cyan pills), distinct from the dark product, tactile and printed rather than floating. | `design-system/threadline-design-dna.json`; `src/app/public.css` scoped to `.tl-public`. |
| 2026-09-09 | **The creative system is the Authority Factory: thread + conveyor.** Thread = idea lineage; machinery = Threadline; weave = market memory. Original cast of humans and machines, never creatures. | A physical process explains the product without exposing internal jargon. | `src/components/factory/*`. |
| 2026-09-09 | **One brand, three intensities**: public expressive · client calm · admin dense. The public system reaches the app only as type, focus rings and stamps. | Operators need density; clients need calm; prospects need story. | Client and admin surfaces unchanged visually except auth screens on paper. |
| 2026-09-09 | **Motion must teach.** Six named animations (belt, thread draw, stamp, pulse, scroll-linked machine, reveal), each with a reduced-motion still. No animation library. | Decorative motion on a page selling calm operations argues against itself. | `public.css` keyframes; `qa:public` verifies zero running animations under reduced motion. |
| 2026-09-09 | **Threadline's own baseline is the regression target.** `qa-baselines/public/` (screenshots + geometry) replaces any reference comparison. | The reference's geometry is not the spec. | `npm run qa:visual` / `qa:visual:compare`. |
| 2026-09-09 | **Claims discipline.** Every factual public statement has a row in `docs/site/CLAIMS_EVIDENCE_LEDGER.md`; synthetic numbers are labelled on the page; no testimonials, logos or case studies until `ProofPermission` grants exist. | No invented proof, ever. | `qa:public` fails on placeholder/overclaim/brand-leak text. |
| 2026-09-09 | **Every-4-weeks language only.** | A four-week period is not a month; thirteen periods a year. | Cadence sweep in `suite-reports`; `qa:public` fails on "monthly". |
| 2026-09-09 | **No rev share at launch; data is rev-share-ready.** | Performance pricing needs explicitly agreed, cash-collected attribution; the model must exist before the pricing does. | `CommercialEvent` eligibility/status/pool fields; `revSharePool()` computes only from confirmed + agreed + collected. |
| 2026-09-09 | **Calculator retained, demoted.** Kept because it costs the founder's own time honestly and never projects revenue; removed from primary nav. | It is a supporting tool, not a destination. | Footer link "Cost of the status quo". |
| 2026-09-09 | **No Results page.** | No client evidence exists; a synthetic-logo Results page would be a lie. | Product proof lives on the homepage, labelled synthetic. |
| 2026-09-09 | **Playbook on the main domain, ungated.** The Founder Authority System — ten original chapters. | Give away something unusually valuable before the ask; gating by email is deferred until email delivery is live. | `/playbook/*`; email capture is a future addition, not a placeholder. |
| 2026-09-09 | **Text-led work starts in editing, never in the recording queue.** | Posts, threads, carousels are written, not recorded. | `isTextLed()`; `sendToRecordingAction` routes by format/platform. |
| 2026-09-09 | **Cold-start diagnosis uses the corpus ladder.** | A new client's first pieces cannot wait for three siblings. | `bandAgainstOwnWork` passes cohort/platform baselines; the reading names its baseline source. |
| 2026-09-09 | **Email and jobs are database-backed, provider-abstracted; storage adds S3 behind the same interface; the rate limiter fails closed when a shared store is required and unreachable.** | Production-shaped without new heavy dependencies. | `src/lib/email`, `src/lib/jobs`, `src/lib/storage/s3.ts`, `src/lib/security/rate-limit.ts`. |
| 2026-09-09 | **Canonical sales scripts are imported verbatim from the draft documents and remain unusable until a person approves them.** | AI may select wording; it may not write it. | `/admin/scripts`; "CANONICAL COPY IMPORT REQUIRED" until approval. |
