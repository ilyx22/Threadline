# Decisions log — public experience and completion pass

Append-only. Each entry: id, date, decision, why, consequence. Ids are stable references
(`DEC-nnn`) used by `HANDOFF.md`, `BRAND_SOURCE_OF_TRUTH.md` and the claims ledger; the next entry
takes the next number.

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|
| DEC-001 | 2026-09-09 | **Birdhouse is a specimen, not a spec.** Reference analysed mechanically (20 widths, computed styles, geometry, motion); principles extracted; every asset, colour, character, framework, claim and sentence refused. | The founder asked for the rigour of a reconstruction system with a different objective: an original Threadline experience. | `reference-analysis/` holds the evidence; `forbidden-to-copy.md` is enforced by a grep in `qa:public`. |
| DEC-032 | 2026-09-24 | **The remaining public pages join the homepage system.** How it works: a statement panel with the busy machine, the six-station line as the interactive stage in a night panel, the seven stages as a ledger of tiles with the founder's three marked, the four gates, the synthetic demonstration as a ten-link chain stamped Illustrative, the closing panel. Apply and the calculator: the statement beside the unchanged form / model inside one white panel. Not found: one centred panel. The sticky apply bar and the schematic tiles of the earlier layout are retired; the diagnostic lives in the Playbook's tools. Nothing behind the form, the booking link or the calculator changed. | The owner: "what about the other pages such as how it works". Every public route should read as one site. | `src/components/marketing-v9/HowItWorks.tsx`, `src/app/(marketing)/{how-it-works,apply,calculator}/page.tsx`, `src/app/not-found.tsx`, `src/styles/marketing-v9/index.css`, `scripts/qa/marketing-v9.ts` |
| DEC-033 | 2026-09-24 | **Generated artwork replaces the code-drawn scenes on the homepage.** After a rendered-page diagnosis (the flat-vector figure vocabulary read as a generic AI explainer; overlaps and clipped labels in the mosaic and the vault band), the three atmospheric moments (hero, visibility gap, closing) are Nano Banana renders from Threadline's own written prompts, and a 17-object flat set carries the station tiles, the burden tiles and the capsules with all labels in HTML. Rules kept: faceless scale figures (pale-blue ovals, no hands, no skin tone), one line weight, the marigold thread as the only warm colour, no text in artwork, no other company's site as a reference. Diagrams with words, states or interaction (bench, expressions line, memory frieze, fit lists) stay code-drawn. | The owner: "i have tons of pics im confused… take over my chrome tab and do it for me", then "complete all of em… be very strict with quality level", then "just continue". | `public/marketing/*`, `docs/design/nano-banana-reference/*`, `src/components/marketing-v9/Home.tsx`, `src/styles/marketing-v9/index.css`, `scripts/qa/marketing-v9.ts` |
| DEC-034 | 2026-09-24 | **Second artwork pass from the owner's review.** Six more generated scenes (the five encounters, the learning bench, the eight-tools bench, the six-station line, the gate, the reading desk) replace the remaining code-drawn scenes on the homepage, How it works, Who it is for and the Playbook; the gap band is shown at its native size; the bench's Change step lifts the failed block out instead of dropping a hook; the Playbook marks chapters read as they are read and carries one object per chapter; motion is applied only where it is honest (plates, captions, objects, the station marker, the block swap) and a traced-thread overlay was rejected. | The owner's itemised review of the first pass. | `public/marketing/*`, `src/components/marketing-v9/{Home,HowItWorks,WhoItsFor,StationLine,Obj}.tsx`, `src/components/marketing-v9/playbook/{Playbook,Chapter,Progress}.tsx`, `src/styles/marketing-v9/index.css`, `scripts/qa/marketing-v9.ts` |
| DEC-035 | 2026-09-24 | **Third review pass: no em dashes on the public site; the interactive bench built from generated pieces; the station line and the station objects at native resolution; the Playbook hero as a chapter map; How it works drops "No client results to show yet".** | The owner's third itemised review. | `src/content/*`, `src/components/marketing-v5/Bench.tsx`, `src/components/marketing-v9/*`, `public/marketing/{bench,objects-big}/*`, `src/styles/marketing-v9/index.css` |
| DEC-036 | 2026-09-25 | **Phone pass and the product identity on the public site.** The frontend freeze is reopened for two owner requests only. (1) The public nav, footer and not-found page use the product identity (the thread mark beside the tracked uppercase wordmark, 20% larger and heavier than the product default). (2) A phone-only pass (759px and under; desktop and tablet untouched) after a part-by-part audit at 390px (`docs/site/PHONE_AUDIT_2026-09-25.md`): panels lose their side inset and gutters shrink (copy width 310px to 350px), body copy drops to 17px at 1.6 line height (about 40 characters a line), tags and labels rise to 12.5px in the soft ink, the hero and gate scenes are cropped to taller frames instead of shown as strips, the five encounters and the six stations become rows instead of a sideways strip and tall tiles, the station line stacks its two halves at full width without the marker, the tool objects shrink so the two tool tiles halve in height, the fit list is one column, and the drawn labels inside the testing bench are hidden under 600px. Apply and Calculator were checked on the live site and unchanged. | The owner, after seeing the live site on a phone: "the phone is too narrow, some stuff hard to read", "audit harder, part by part", "happy to change structure of boxes for the mobile only". | `src/styles/marketing-v9/index.css`, `src/components/marketing-v5/Nav.tsx`, `src/styles/marketing-v5/page.css`, `docs/site/PHONE_AUDIT_2026-09-25.md` |
| DEC-002 | 2026-09-09 | **Public visual direction: warm light canvas, editorial serif + plain sans + mono labels, paper cards with ink outlines and hard offset shadows.** | Distinct from the reference (sky blue / navy / cyan pills), distinct from the dark product, tactile and printed rather than floating. | `design-system/threadline-design-dna.json`; `src/app/public.css` scoped to `.tl-public`. |
| DEC-003 | 2026-09-09 | **The creative system is the Authority Factory: thread + conveyor.** Thread = idea lineage; machinery = Threadline; weave = market memory. Original cast of humans and machines, never creatures. | A physical process explains the product without exposing internal jargon. | `src/components/factory/*`. |
| DEC-004 | 2026-09-09 | **One brand, three intensities**: public expressive · client calm · admin dense. The public system reaches the app only as type, focus rings and stamps. | Operators need density; clients need calm; prospects need story. | Client and admin surfaces unchanged visually except auth screens on paper. |
| DEC-005 | 2026-09-09 | **Motion must teach.** Six named animations (belt, thread draw, stamp, pulse, scroll-linked machine, reveal), each with a reduced-motion still. No animation library. | Decorative motion on a page selling calm operations argues against itself. | `public.css` keyframes; `qa:public` verifies zero running animations under reduced motion. |
| DEC-006 | 2026-09-09 | **Threadline's own baseline is the regression target.** `qa-baselines/public/` (screenshots + geometry) replaces any reference comparison. | The reference's geometry is not the spec. | `npm run qa:visual` / `qa:visual:compare`. |
| DEC-007 | 2026-09-09 | **Claims discipline.** Every factual public statement has a row in `docs/site/CLAIMS_EVIDENCE_LEDGER.md`; synthetic numbers are labelled on the page; no testimonials, logos or case studies until `ProofPermission` grants exist. | No invented proof, ever. | `qa:public` fails on placeholder/overclaim/brand-leak text. |
| DEC-008 | 2026-09-09 | **Every-4-weeks language only.** | A four-week period is not a month; thirteen periods a year. | Cadence sweep in `suite-reports`; `qa:public` fails on "monthly". |
| DEC-009 | 2026-09-09 | **No rev share at launch; data is rev-share-ready.** | Performance pricing needs explicitly agreed, cash-collected attribution; the model must exist before the pricing does. | `CommercialEvent` eligibility/status/pool fields; `revSharePool()` computes only from confirmed + agreed + collected. |
| DEC-010 | 2026-09-09 | **Calculator retained, demoted.** Kept because it costs the founder's own time honestly and never projects revenue; removed from primary nav. | It is a supporting tool, not a destination. | Footer link "Cost of the status quo". |
| DEC-011 | 2026-09-09 | **No Results page.** | No client evidence exists; a synthetic-logo Results page would be a lie. | Product proof lives on the homepage, labelled synthetic. |
| DEC-012 | 2026-09-09 | **Playbook on the main domain, ungated.** The Founder Authority System — ten original chapters. | Give away something unusually valuable before the ask; gating by email is deferred until email delivery is live. | `/playbook/*`; email capture is a future addition, not a placeholder. |
| DEC-013 | 2026-09-09 | **Text-led work starts in editing, never in the recording queue.** | Posts, threads, carousels are written, not recorded. | `isTextLed()`; `sendToRecordingAction` routes by format/platform. |
| DEC-014 | 2026-09-09 | **Cold-start diagnosis uses the corpus ladder.** | A new client's first pieces cannot wait for three siblings. | `bandAgainstOwnWork` passes cohort/platform baselines; the reading names its baseline source. |
| DEC-015 | 2026-09-09 | **Email and jobs are database-backed, provider-abstracted; storage adds S3 behind the same interface; the rate limiter fails closed when a shared store is required and unreachable.** | Production-shaped without new heavy dependencies. | `src/lib/email`, `src/lib/jobs`, `src/lib/storage/s3.ts`, `src/lib/security/rate-limit.ts`. |
| DEC-016 | 2026-09-09 | **Canonical sales scripts are imported verbatim from the draft documents and remain unusable until a person approves them.** | AI may select wording; it may not write it. | `/admin/scripts`; "CANONICAL COPY IMPORT REQUIRED" until approval. |
| DEC-017 | 2026-09-09 | **Exact service pricing is not disclosed on the public website.** The £2,500 implementation / £2,500 every 4 weeks / 12-week initial engagement figures stay the internal source of truth (HANDOFF §16b, unchanged) and are discussed during the qualified sales process. Public copy never uses "starting from £X", fake discounts, scarcity or urgency. | Pricing on the page invites comparison before diagnosis and anchors the conversation on cost rather than the expensive problem; a founding-client programme sells on fit. | Claims-ledger row `C-PRICE`: VERIFIED as internal fact, PUBLIC USE NOT PERMITTED, removed from the site 2026-09-09 (removal from `src/content/public-site.ts` landing in the concurrent public-site pass). `BRAND_SOURCE_OF_TRUTH.md` "Never say" and "Public surface rules" updated. |
| DEC-018 | 2026-09-09 | **Platform safety doctrine: automate publishing through official rails; do not automate human social behaviour.** Four action classes — PUBLISH and ANALYTICS via official/authorised APIs or supported providers, RESEARCH from permitted public/provider sources with no authenticated scraping or access bypass, ENGAGEMENT human by default. Never: browser bots, Selenium/Playwright social operation, cookie/session-token automation, stored client social passwords, auto-like, engagement pods, follow/unfollow automation, connection farming, bulk unsolicited replies/DMs/comments, recommendation or location manipulation, evasion of rate limits, app review or restrictions. OAuth with scoped, revocable permissions; secrets encrypted at rest before real credentials; connector provenance records platform, account, content, provider, action, timestamp, authorisation state, error state, rate state. | Account bans and platform sanctions land on the client, not on Threadline; a growth system that depends on evading the platform is not a system the client can keep. | `HANDOFF.md` §16i and DO-NOT-BREAK 33; `docs/PLATFORM_APPLICATIONS.md` doctrine paragraph; `FUTURE_BACKLOG.md` "Explicitly not planned"; `assertCanPublish`, SSRF/login-wall guards and the research quarantine are the enforcement points. |
| DEC-019 | 2026-09-09 | **Retired: any recommendation to use a stable US SIM/eSIM plus a VPN to influence organic recommendation geography.** Superseded; must not be built or recommended. Preferred: US buyer problems, US market terminology and examples, genuine US relationships and collaborations, appropriate posting windows, audience-geography measurement, and legitimate paid geo-targeting later where commercially justified. | It is location manipulation under DEC-018, and a reach gain built on it is neither honest nor durable. | No Markdown document in this repository carried the idea on 2026-09-09; it survives only in older external resource documents and is recorded here so it cannot be reintroduced from them. `HANDOFF.md` §13.20. |
| DEC-020 | 2026-09-09 | **Cadence doctrine.** Content cadence is determined by platform, account maturity, quality, audience tolerance, native/API constraints, commercial evidence and founder/delivery load. Extreme legacy posting frequencies are not customer promises; they may remain historical or internal hypotheses. One root idea may produce several native expressions; derivatives are not new root ideas. Frequency increases only where quality, buyer relevance and commercial signal remain intact. | The constraint diagnosis already says volume worsens five of nine dimensions; a promised frequency would override the diagnosis with a number nobody measured. | `HANDOFF.md` §16f "Content cadence doctrine"; `ContentRoot` lineage counts roots, not derivatives; `BRAND_SOURCE_OF_TRUTH.md` forbids frequency promises. |
| DEC-021 | 2026-09-09 | **Offer direction.** Threadline is not a video production company and not a platform buffet. It runs a low-friction founder authority + qualified-demand system around what the client already knows: video-led where it adds information or trust, text-native and platform-prescribed otherwise. The platform mix is a prescription after diagnosis, not the headline offer. LinkedIn may be an anchor hypothesis for the expert-led B2B wedge but is not hard-coded as universal strategy. | Selling a platform or a production service invites comparison on price per video; selling the system around existing expertise is the thing only Threadline does. | `HANDOFF.md` §16b "Positioning guardrails"; `BRAND_SOURCE_OF_TRUTH.md` "Never say" and "Public surface rules"; no channel is doctrine in code (`funnel.test.ts`). |
| DEC-022 | 2026-09-09 | **Public-site visual direction (in progress).** A visual pass toward roughly 70% premium editorial / 20% interactive product visualisation / 10% playful illustrated character, by component-first clean-room reconstruction of selected reference components (Birdhouse for composition, Hydra for interaction). Content, sections and section order preserved. The Authority Factory concept is kept, expressed as system visualisation rather than a literal cartoon factory. | The rebuild proved the system; the next pass raises its polish without changing what it says. | Being implemented in a separate process; nothing here describes its implementation. DEC-001, DEC-006 and DEC-007 still apply (forbidden-copy grep, own baseline, claims ledger). `qa-baselines/public/` and `docs/design/*` are re-baselined when it lands. `HANDOFF.md` §16j. |
| DEC-023 | 2026-09-09 | **Homepage information architecture and object language (the captivation pass — implemented).** The homepage order is: hero → commercial problem → desired outcome (market memory) → founder burden → the Authority Factory (centrepiece; the founder appears only at Input / Record / Approve / Sell) → one idea, the right expressions → attention to commercial movement → EXPECTED / ACTUAL / WHY / CHANGE / RETEST → the 12-week progression as one continuous visual (never four card boxes) → the honest comparison (Ghostwriter / Content agency / In-house / Threadline, capability wording only) → fit → the ask. Station-by-station detail, the proof ledger and the symptom list live on How it works. Line diagrams are replaced by filled 2.5D objects (chips, cards, tokens, tiles, markers, chambers, modules); no glass, glow or dark SaaS. The platform is a component, not the category: no platform is named in a headline. | The restraint-pass page explained the system inside-out (stations before tension) and leaned on thin-line diagrams; a cold visitor could not say what Threadline was in thirty seconds. | Supersedes the "in progress" note in DEC-022 (its constraints — DEC-001, DEC-006, DEC-007 — still apply). Every approved sentence preserved (`docs/design/CAPTIVATION_PASS_2026-09-09.md` §3). New copy under `HOME_V3` / `DIAGNOSTIC` in `src/content/public-site.ts`; new claims rows C-COMPARISON, C-LEARNING-ILLUSTRATIVE, C-THESIS-ILLUSTRATIVE, C-FOUNDER-TOUCHPOINTS, C-ROUTE-STEPS. Pre-pass baseline in `qa-baselines/public-pre-captivation-2026-09-09/`. `HANDOFF.md` §16j. |
| DEC-024 | 2026-09-11 | **Reference builds are quarantined, never integrated.** The Birdhouse-derived reconstruction copied into the repository (`thebirdhouse/`) stays on disk as a reference, is git-ignored and excluded from the TypeScript project, and nothing in it (code, media, copy, the imported third-party playbook) is imported, built, served or committed. Product titles adopt the public serif (Fraunces) while the product keeps its dark palette and density (refines DEC-004). | Committing it would distribute copied media and third-party content and would nest a second git repository; the brand is carried into the product by type, not by marketing layouts. | `.gitignore`, `tsconfig.json`; `docs/design/THREADLINE_PUBLIC_EXPERIENCE_HANDOVER.md` §7–§8; `HANDOFF.md` §16j. |
| DEC-025 | 2026-09-12 | **The marketing experience authored in the `thebirdhouse/` workspace (tags `threadline-marketing-v1`…`v3`) is the public site.** It is integrated by a generated port, not by copying the reference: `scripts`-free Python in the session scratchpad flattens the workspace's hand-authored stylesheets under the production `.tl-public` scope into `src/styles/marketing/marketing.css`, renames the two class families that collide with Tailwind utilities, maps fonts onto `next/font`; the twelve components, content and motion tokens live under `src/components/marketing-v4/`, `src/content/marketing-site.ts`, `src/content/marketing-tokens.ts`. The Birdhouse reconstruction itself stays quarantined (DEC-024 stands); `thebirdhouse/` stays git-ignored. Inner pages keep their components and take the same palette through the `.tl-public` tokens. The public category is "a managed authority system for expert-led firms" — never a content, LinkedIn, ghostwriting or growth category — and the Playbook is a product: chapters plus two tools (diagnose, model). | The owner drove three passes in the workspace and directed a final pass that preserves the strongest work, finishes the Playbook and ships. | `src/app/(marketing)/*`, `src/styles/marketing/*`, `src/components/marketing-v4/*`, `docs/design/marketing-v4/` |
| DEC-026 | 2026-09-19 | **Public site v5: an illustrated Threadline world replaces the v4 port.** The owner rejected v4's rendered result (a component vocabulary on a cream canvas, card grids, abstract tokens, reveal-gated blanks) and directed a full visual rebuild. v5 is an original world: an editorial authority workshop connected by one continuous thread, drawn in code (`src/components/marketing-v5/art/`), with a warmer palette (ink-navy, parchment, powder sky, mint / coral / lilac / butter, one marigold accent) and twelve scenes that recompose on phones. The Birdhouse reconstruction was used only as a benchmark for composition, scale, pacing and motion discipline (`docs/design/V5_REFERENCE_SKELETON.md`); nothing was copied. Copy meaning is preserved; the copy source is `src/content/marketing-v5.ts`. Nothing is hidden before it enters the viewport; reduced motion renders authored final states. DEC-024 and DEC-025's category rule stand; DEC-025's port mechanism is retired with v4. | The v4 site explained Threadline but did not create desire, personality, visual memory or delight; the owner set the reference's calibre as the bar. | `src/app/(marketing)/*`, `src/components/marketing-v5/*`, `src/styles/marketing-v5/*`, `src/content/marketing-v5.ts`, `docs/design/V5_*.md`, `THREADLINE_FRONTEND_V5_HANDOFF.md` |
| DEC-027 | 2026-09-19 | **v5 review pass: shorter, plainer, purchase-oriented; the illustrated world kept.** The thirteen-scene first build became eight scenes: the tag strip, the Expressions arc, the movement street, the twelve-week loom and the standalone comparison were removed or folded (market memory into the problem section, the abacus into fit); a labelled illustrative "What you receive" engagement example sits before the mechanism; the workshop lost its sticky track and shows the whole bench with six named captions at once (numbered 44px station buttons; ←/→/Home/End on the button group; a clamped phone pan); copy was rewritten plainer around what Threadline does, what the founder does, what is delivered and why the loop matters. The palette was muted and headings moved to Instrument Serif for restraint. Doctrines unchanged: no pricing, no proof, no promised numbers, AI outside the public category, the platform a component. | The first v5 build was ~17k px tall at 1440 with repeated ideas (two comparison passages, two loop passages) and three verified phone defects in the workshop; a buyer needs to see what is delivered early. | `src/app/(marketing)/page.tsx`, `src/components/marketing-v5/*`, `src/content/marketing-v5.ts`, `src/styles/marketing-v5/*`, `scripts/qa/marketing-v5.ts`, `docs/design/V5_VISUAL_QA_REPORT.md` |
| DEC-028 | 2026-09-21 | **v5 visual-quality upgrade: the illustrated world is redrawn as a restrained editorial system.** Ideas, copy and sequence unchanged. Figures become faceless architectural scale figures (no faces, hair, hands, skin tones or expressions); every outline is one 1.6-unit line; radii drop to 1–4 units; the thread thins and ends deliberately in every scene; each scene uses paper, sky, parchment, wood and one lilac with mint and coral reserved for states; speech bubbles, the "?" mark, talking dashes and sparkle marks are removed; every perpetual motion loop is removed and only functional motion remains (draw, settle, carrier, one signal return, one conveyor pass, the swapped part). Instrument Serif + Inter stay; Fraunces, the wonky face and JetBrains Mono stay out of the marketing illustrations. Labels fit their objects by measurement or `textLength`; crops are intentional (`clipPath`, a `compact` vault), never a global `overflow: hidden` on `.v5-art`. | The owner approved the concepts but rejected the execution as children's animation / clip art: thick toy outlines, sugary pastels, cute faces, bubbles, stars, looping motion, clipped labels. | `src/components/marketing-v5/art/*`, `src/components/marketing-v5/Bench.tsx`, `src/styles/marketing-v5/{art,motion,page}.css`, `docs/design/V5_*.md`, `THREADLINE_FRONTEND_V5_HANDOFF.md`, `docs/design/v5/after/` |
| DEC-029 | 2026-09-24 | **Homepage rebuilt as a premium editorial page; the illustrated world and the v7 interim leave the front page.** The founder-feedback brief (70% premium editorial, 20% product visualisation, 10% tactile) and the owner's rejection of the v7 concept board set the direction: warm neutral ground, paper sheets with hairlines, deep ink type at a measured size, one terracotta accent, no illustrations, characters, coloured panels, gradients, dashboards or dense diagrams. Eight parts in one narrative plus a closing call; two interactive objects (the workshop rail, the learning ledger) that read in full without scripting; motion limited to entry settles and the rail marker. Copy ideas protected; operating detail limited to capabilities the application has. `/how-it-works`, `/who-its-for` and `/playbook` keep the detailed cadence, comparison and methodology. | The interim v7 page looked like a low-quality concept board and the v5 world read as children's animation; a strategy buyer must find the page intentional, authored and commercially credible within seconds. | `src/components/home/*`, `src/content/home.ts`, `src/styles/home/home.css`, `src/app/(marketing)/{page,layout}.tsx`, `scripts/qa/marketing-home.ts`, `THREADLINE_FRONTEND_V5_HANDOFF.md` |
| DEC-030 | 2026-09-24 | **Homepage second pass: the reference's composition with the illustrated Threadline world.** The dry editorial rebuild (DEC-029) was rejected as a regression. The front page now uses the mechanics the owner keeps pointing at in the Birdhouse example (pale canvas, big white panels inset from the viewport, a giant serif statement with a scene running off the panel, pill actions, an italic audience line, pastel tiles carrying artwork, a full-bleed band with the next panel riding over it, a ticker, a mosaic, a landscape and a giant wordmark to end on) and brings the v5 illustrated scenes back into every section, retoned brighter and warmer through the kit's variables. Clean-room rule unchanged: composition and behaviour only. DEC-029's copy and section story stand. | The owner: "i dont like the frontend man i feel we have regressed, read our progress throughout the project and look at the birdhouse example and re-do". The page must feel like the reference's warmth and choreography, not a concept board or a dry editorial. | `src/components/marketing-v9/*`, `src/styles/marketing-v9/index.css`, `src/components/marketing-v5/art/{WorkshopArt,kit}.tsx`, `src/app/(marketing)/{page,layout}.tsx`, `scripts/qa/marketing-v9.ts`, `THREADLINE_FRONTEND_V5_HANDOFF.md` |
| DEC-031 | 2026-09-24 | **Who it is for and the Playbook join the homepage system; the Playbook becomes one interactive page.** Who it is for: a statement panel with the gate, the seven-point profile as a ledger of tiles, the fit in one look, the closing panel. The Playbook: a start button with a time promise (about 20 minutes, fully interactive), a maxims marquee, a sticky rail of ten chapter marks with read-state kept in the browser, ten chapters each with the idea, the card to turn, the thing to do today and an object to use (crates to tap, the one-sentence test, drawers, a thesis-or-topic deck, a room picker, an encounter scrubber, the evidence ladder, an expectation card, the learning bench, promise flip cards), the two tools, the three periods stated without promises, the hand-over. The Birdhouse LinkedIn Playbook was read for its interaction vocabulary only; no content, artwork, numbers, claims or copy carry over. | The owner: "the second tab ie who is it for does not match the design style. also can u use linkedin.thebirdhouse.co to help design an interactive playbook for me". | `src/components/marketing-v9/WhoItsFor.tsx`, `src/components/marketing-v9/playbook/*`, `src/content/playbook.ts`, `src/app/(marketing)/{who-its-for,playbook}/**`, `src/styles/marketing-v9/index.css`, `scripts/qa/marketing-v9.ts` |
