# Forbidden to copy — Hydra (workwithhydra.com)

Everything below was observed as a normal visitor. It is analysed, never reused. The public QA brand-leak sweep (`npm run qa:public`) greps every public route for the names in this list.

| Category | Specific items | Rule |
|---|---|---|
| Brand | "Hydra", the blackletter "H" wordmark, `workwithhydra.com` | Never appears in Threadline code, content or assets |
| Copy | Every headline and sentence: "We scale agencies + B2B service companies…", "Every service business has exactly one constraint…", "Scale is a game of inputs and outputs", "Numbers with a client attached to them", "Bring your pipeline and your P&L…", the results disclaimer | Not paraphrased, not reused |
| Frameworks | The four constraints (Acquisition / Conversion / Delivery / Margin), "throughput capped here", "inputs / system / output", "find your constraint" quiz, "built for you / with you" | Threadline uses its own five-station diagnosis and its own division of labour |
| Proof | Client names and logos (the logo wall), revenue and growth figures, Trustpilot reviews, "300+", "9.2/10", "30+" | Threadline shows no client proof until a client agrees to share it (claims ledger) |
| Palette | #F4F3F0 / #ECEBE6 canvas, #12121A ink, #4D26DB violet, #FFD84D butter, #F58A1E orange | Threadline keeps its linen/ink/ember/signal palette |
| Type | Archivo, the italic-violet emphasis device, the hand-drawn violet underline | Threadline uses Fraunces + Inter + JetBrains Mono; emphasis is the marker highlight |
| Assets | The funnel/pipe hero diagram, the gauge, all SVG glyph art, photos | Threadline draws its own thread/spool/routing/inspection glyphs |
| Offer | Pricing, engagement lengths ("8 weeks", "six months"), "equity partnership" | Threadline's commercial terms are its own and are not published |

What *is* permitted: the observable engineering — geometry, spacing, type scale relationships, state logic, transitions, responsive rearrangement — reconstructed clean-room with neutral placeholders (`reference-analysis/clones/*`) and then mutated into Threadline.
